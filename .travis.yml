language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-am4' ]; then cd .. && eval "mv $currentfolder generator-am4" && cd generator-am4; fi

